#include <stdio.h>
#include <string.h>
int main() {
char mot[] = "if(x11==y1) z=y1;"; // Déclare le mot u
// Calcul de la longueur du mot
int longueur = strlen(mot);
// Affichage de la longueur du mot
printf("La longueur du mot u est : %d\n", longueur);
return 0;
}
